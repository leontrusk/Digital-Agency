import classes from './Footer.module.css';

const Footer = (props) => {
    return (
        <div className={classes.main}>
            <h1 className={classes.text}>@All rights reserved</h1>
</div>
    );
}

export default Footer;