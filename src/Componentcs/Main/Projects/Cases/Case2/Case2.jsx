import classes from './Case2.module.css';

const Case2 = (props) => {
    return (
        <div className={classes.case}>
            <div className={classes.image}></div>
            <h1 className={classes.title}>Web Design for plant’s store</h1>
            <div className={classes.icon}></div>
        </div>
    );
}

export default Case2;